---
export interface Props {
    class?: string;
    id?: string;
}

const { class: className, id } = Astro.props;
---

<section
    id={id}
    class={`w-full max-w-7xl mx-auto px-4 md:px-6 lg:px-8 scroll-mt-24 ${className}`}
>
    <slot />
</section>
